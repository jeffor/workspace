1 数字:
	mongodb支持三种数字类型: 32位整型, 64位整型, 64位浮点型
	shell中只能表示64位浮点型

2 日期:
	new Date(long time);
	shell 中会将日期转换成字符串,但是字符串和日期无法相互匹配

3 数组:
	["data", 12]

4 内嵌文档:
	{...}

5 _id:
	mongodb中存储的文档必须有一个_id，_id可以为任意类型，默认为
	ObjectId类型。
	ObjectId类型 存储12字节数据，每个字节保存2位16进制数值，结构：
	时间戳(4)|机器(3)|PID(2)|计数器(3)
